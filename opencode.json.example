{
  "$schema": "https://opencode.ai/config.json",
  "formatter": {
    "markdownlint": {
      "command": ["npx", "--yes", "markdownlint-cli2", "--fix", "$FILE"],
      "extensions": [".md"]
    }
  }
}

---
colors:
  primary: "#1a56db"
  neutral: "#6b7280"
typography:
  body:
    fontFamily: "Inter"
    fontSize: 16
---

# DESIGN.md (fixture)

Fixture design system used by the spec-sharpening contract test. Defines the
tokens `colors.primary`, `colors.neutral`, and `typography.body` for resolution
checks; `colors.brandBlue` is intentionally undefined.
